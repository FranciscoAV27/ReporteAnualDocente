spring.application.name=ReporteAnualDocente

# Configuración de MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/reporte_anual_docente?useSSL=false&serverTimezone=America/Mexico_City
spring.datasource.username=root
spring.datasource.password=my2003sql
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA (Hibernate)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT (configuración para futuro)
jwt.secret=tu_clave_secreta_muy_larga_y_segura_para_firmar_jwt
jwt.expiration.access=3600000      # 1 hora en milisegundos
jwt.expiration.refresh=86400000     # 24 horas

# Logging
logging.level.org.springframework.security=DEBUG